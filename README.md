Para correr el proyecto de manera local, en la raiz del repositorio correr "npm install" para instalar dependencias
luego de que se instalen, se ejecuta el comando "npm run dev" para correr el proyecto
Y ya solo quedaria abrir el local host que se dispone